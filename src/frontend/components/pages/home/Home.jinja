{#def current_user=None #}
<layout.Page title="Home" current_user={{ current_user }} active="home">
  <div class="home">
    <h1>Home</h1>
    <p style="color: var(--clr-text-secondary);">Welcome back, {{ current_user.name }}.</p>
  </div>
</layout.Page>
