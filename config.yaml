database:
  user: app_user
  host: localhost
  port: 5432
  db: backend

migrator:
  user: migrator_user
  host: localhost
  port: 5432
  db: backend

frontend:
  enabled: true
  components_dir: components
  static_dir: static
