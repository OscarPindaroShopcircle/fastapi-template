database:
  user: app_user
  host: localhost
  port: 5432
  db: backend

migrator:
  user: migrator_user
  host: localhost
  port: 5432
  db: backend

auth:
  bootstrap_admin_email: oscar@shopcircle.co
  redirect_uri: http://localhost:8000/auth/callback

auth:
  bootstrap_admin_email: oscar@shopcircle.co
  redirect_uri: http://localhost:8000/auth/callback

frontend:
  enabled: true
  components_dir: src/frontend/components
  static_dir: src/frontend/static
